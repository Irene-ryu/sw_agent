# git 설정
git config --global user.name Irene-ryu
git config --global user.email "사용자 이메일"

# git 설정 확인
git config --list

# git initial connection
git init

git remote add origin https://github.com/Irene-ryu/sw_agent

